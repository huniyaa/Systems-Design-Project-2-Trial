{
  "version": 2,
  "buildCommand": "npm install && npx prisma generate",
  "env": {
    "DATABASE_URL": "@database_url"
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/public/$1"
    }
  ]
}
